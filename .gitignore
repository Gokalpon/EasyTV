# ══════════════════════════════════════════════════
# EasyTV .gitignore
# Build Infrastructure Configuration
# ══════════════════════════════════════════════════

# Build Output - www/ is generated by build script
www/
www-dist/

# Capacitor Build Artifacts
ios/App/App/public/build/
ios/App/App/public/node_modules/

# Dependencies
node_modules/
package-lock.json
www/package-lock.json

# IDE & OS
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp
*.swo

# Temporary files
.temp/
.tmp/
*-backup.*

# Credentials (NEVER commit these)
.env
.env.*
*.env
credentials.json
*.pem
*.key

# Build caches
.cache/
.buster/
.swc/

# Production secrets (exclude during sync)
# supabase_setup.sql is SAFE to commit (DDL only, no secrets)
